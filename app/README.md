# App (PWA)

Vite + React + TypeScript. The eligibility engine lives in `src/engine/` as a
pure, unit-tested module (inputs: memberships + location; output: per-museum
Free/50%/Verify/Not-covered + recommended association). See `docs/ARCHITECTURE.md`
§6 and §8. Not implemented yet.
