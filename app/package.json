{
  "name": "museum-reciprocal-finder-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PWA frontend for Museum Reciprocal Finder (engine + UI).",
  "scripts": {
    "test": "node --import tsx --test src/engine/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
