name: Update reciprocal data
on:
  schedule:
    - cron: "0 6 1 * *"   # 06:00 UTC on the 1st of each month
  workflow_dispatch: {}    # manual trigger
permissions:
  contents: write
jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      # - run: pip install -r pipeline/requirements.txt
      # - run: python pipeline/build.py          # scrape -> normalize -> geocode -> dedupe -> validate
      # - name: Commit refreshed data
      #   run: |
      #     git config user.name  "data-bot"
      #     git config user.email "data-bot@users.noreply.github.com"
      #     git add data/*.json data/raw
      #     git commit -m "chore(data): monthly refresh" || echo "no changes"
      #     git push
      - run: echo "Pipeline not implemented yet — see docs/ARCHITECTURE.md section 7."
